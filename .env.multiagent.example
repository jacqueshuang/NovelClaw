# MultiAgent 启动示例。复制到 apps/multiagent/.env 后再按需修改。

# 模型与 Provider
# [建议填写] OPENAI_API_KEY：使用 OpenAI 兼容模型时填写。当前程序也支持 `LLM_API_KEY`、`DEEPSEEK_API_KEY` 或 `CODEX_API_KEY` 作为实际启动密钥。
OPENAI_API_KEY=your-openai-api-key-here
# [可选] ANTHROPIC_API_KEY：当前仓库主启动流程默认不直接读取该变量；仅当你的外层代理或兼容层自行消费它时再填写。
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# 数据存储
# [建议保留默认] DATABASE_URL：应用数据库地址。默认使用 local_web_portal/data/app.db。
DATABASE_URL=sqlite:///./local_web_portal/data/app.db

# 访问与入口
# [建议保留默认] HOST：服务监听地址。本地启动通常保持 0.0.0.0。
HOST=0.0.0.0
# [建议保留默认] PORT：MultiAgent 默认端口，对应本地入口 8011。
PORT=8011
