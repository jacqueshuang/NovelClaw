# Auth Portal 启动示例。复制到 apps/auth-portal/.env 后再按需修改。

# 访问与入口
# [建议保留默认] HOST：服务监听地址。本地启动通常保持 0.0.0.0。
HOST=0.0.0.0
# [建议保留默认] PORT：Auth Portal 默认端口，对应本地入口 8010。
PORT=8010

# 数据存储
# [建议保留默认] DATABASE_URL：认证门户数据库地址。默认使用 local_web_portal/data/app.db。
DATABASE_URL=sqlite:///./local_web_portal/data/app.db

# 会话与安全
# [必填] SECRET_KEY：用于会话签名与安全保护。生产环境必须替换为随机长字符串。
SECRET_KEY=your-secret-key-here-change-in-production
